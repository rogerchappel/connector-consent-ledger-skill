# Release Candidate Review

This branch is the initial public release-candidate review surface for `connector-consent-ledger-skill`.

## Reviewer Checklist

- Confirm `review` is dry-run only.
- Confirm `record` writes only to the requested local JSONL ledger.
- Confirm blocked and ask-first states are visible in Markdown and JSON output.
- Confirm verification commands in `docs/RELEASE_CANDIDATE.md` pass locally.
