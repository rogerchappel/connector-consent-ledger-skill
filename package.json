{
  "name": "connector-consent-ledger-skill",
  "version": "0.1.0",
  "description": "Local-first consent ledgers for connector action plans.",
  "type": "module",
  "bin": {
    "connector-consent-ledger": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js test/*.test.js",
    "build": "node --check src/*.js test/*.test.js",
    "smoke": "node src/cli.js review fixtures/mixed-actions.json --format markdown"
  },
  "keywords": [
    "agent-skill",
    "connectors",
    "approval",
    "dry-run",
    "ledger"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "src",
    "fixtures",
    "docs",
    "SKILL.md",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/connector-consent-ledger-skill.git"
  }
}
